# The backend's versioned API root.
# NOTE: 127.0.0.1 rather than localhost, and port 8010 rather than 8000 --
# see the README for why both matter on this machine.
VITE_API_BASE_URL=http://127.0.0.1:8010/api/v1
